# Use the official Python image as a base
FROM python:3.10-slim

# Set the working directory
WORKDIR /app

# Copy the verifier selector script into the container
COPY verifierSelector.py ./

# Install required Python packages
RUN pip install redis

# Set the default command to run the selector
CMD ["python", "verifierSelector.py"]
